import React from "react";
import ReactDOM from "react-dom/client";
import DesktopBootstrap from "./DesktopBootstrap.jsx";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DesktopBootstrap />
  </React.StrictMode>
);
